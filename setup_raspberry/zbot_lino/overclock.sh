#!/bin/bash
# modify /boot/firmware/config.txt, add below lines
# arm_freq=2100
# gpu_freq=750
# over_voltage=6
# force_turbo=1
#
# copy boot-firmware-config.txt to /boot/firmware/config.txt
sudo cp boot-firmware-config.txt /boot/firmware/config.txt
